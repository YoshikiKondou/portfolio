<h2>トレーニング編集</h2>
<div class="workout new">
  <% if flash[:failure] %>
    <p class="failure"><%= flash[:failure] %></p>
  <% end %>
  <%= render 'layouts/shared/error', obj: @workout %>
  <%= form_with model: @workout do |f| %>
     <div class="form-group">
      <%= f.label :start_time, "日付：", class: "col-form-label" %>
      <%= f.datetime_field :start_time, include_blank: "選択して下さい", name: "workout[start_time]", class: "form-control form-control-sm" %>
    </div>
    <div class="form-group">
      <%= f.label :body_weight, "体重：", class: "col-form-label" %>
      <%= f.number_field :body_weight, step: "0.1", name: "workout[body_weight]", class: "form-control form-control-sm" %>kg
    </div>
    <div class="form-group">
      <%= f.label :part, "部位：", class: "col-form-label" %>
      <%= f.select :part, ["胸", "腕", "背中", "肩", "脚", "四頭", "ハム、尻部", "腹筋"], include_blank: "選択して下さい", class: "form-control form-control-sm" %>
    </div>
    <p>-----------------------------------------------------</p>
    <div class="form_count" id="form_list">
      <%= f.fields_for :workout_menus do |form| %>
        <%= render "workouts/edit_workout_menu_fields", f: form %>
      <% end %>
    </div>
    <div id="detail-association-insertion-point"></div>
    <%= link_to_add_association "+メニューを追加", f, :workout_menus, class: "btn btn-secondary btn-block", data: { association_insertion_node: '#detail-association-insertion-point', association_insertion_method: 'after'} %>
    <div class="form-group">
      <%= f.label :memo, "メモ：", class: "col-form-label" %>
      <%= f.text_area :memo, rows: "3", name: "workout[memo]", class: "form-control form-control-sm" %>
    </div>
  
    <p>-----------------------------------------------------</p>

    <h4>体調チェック ※ 1 : 悪い 〜 5 : 良い</h4>
    <div class="form-group">
      <%= f.label :sleep, "睡眠：", class: "col-form-label" %>
      <%= f.select :sleep, [*(1..5)], class: "form-control form-control-sm" %>
    </div>
    <div class="form-group">
      <%= f.label :eat, "食事：", class: "col-form-label" %>
      <%= f.select :eat, [*(1..5)], class: "form-control form-control-sm" %>
    </div>
    <div class="form-group">
      <%= f.label :motivation, "モチベーション：", class: "col-form-label" %>
      <%= f.select :motivation, [*(1..5)], class: "form-control form-control-sm" %>
    </div>
    <div class="form-group">
      <%= f.label :fatigue, "疲労：", class: "col-form-label" %>
      <%= f.select :fatigue, [*(1..5)], class: "form-control form-control-sm" %>
    </div>
    <div class="form-group">
      <%= f.label :muscle, "筋肉の状態：", class: "col-form-label" %>
      <%= f.select :muscle, [*(1..5)], class: "form-control form-control-sm" %>
    </div>
    <br>
    <%= f.submit '更新する'%>
  <% end %>
</div>
